/**
 * @constant
 * @default Math.PI * (3 - Math.sqrt(5));
 */
export const GOLDEN_ANGLE = Math.PI * (3 - Math.sqrt(5));
/**
 * @constant
 * @default _2PI = 2 * Math.PI;
 */
// eslint-disable-next-line no-underscore-dangle
export const _2PI = 2 * Math.PI;
